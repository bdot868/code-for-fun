<div class="location-dropdown">
  <select name="list" id="list" accesskey="target">
      <option value='none' selected>Select a Location</option>
      <option value="/fremont-sp">Fremont 42</option>
      <option value="/fremont-city">Fremont City Rec Center</option>
      <option value="/lgs">LGS</option>
      <option value="/menlo_park">Menlo Park</option>
      <option value="/milpitas">Milpitas</option>
      <option value="/mountain-view-1">Mountain View</option>
      <option value="/palo_alto">Palo Alto</option>
      <option value="/redwood_city">Redwood City</option>
      <option value="/sunnyvale">Menlo Park</option>
      <option value="/union-city">Union City</option>
    	<option> More Locations coming soon! </option>
    <select>
    <input class="btn" type=button value="Go" onclick="goToNewPage()" />
</div>

$btns.click(function(){
		modal.style.display = "block";
		var $row = $(this).parent().parent()
		var imgSrc = $row.find('#all-info').children().first().children().first().attr('src')
		var courseName = $row.find('#all-info').children().eq(1).text()
		var desc = $row.find('#all-info').children().eq(2).first().children().text()
		var url = $row.find('#all-info').children().eq(3).text()
		var video = $row.find('#all-info').children().eq(4).first().children().html()
		// console.log(desc)
		console.log(video)
		// console.log(courseName)
		// console.log(url)
		$('#course-title').text(courseName)

		// var obj = document.createElement('object');
		// obj.data = url;
		// obj.id = 'checkout'
		// obj.type = 'text/html';
		// obj.standby = 'Product Page is loading...'
		// obj.height = '400px';
		// obj.width = '100%';
		// obj.style = "overflow:auto"
		// $('#modal-info').html(obj)
		// $('#enrollBtn').attr("href", url)
		$("#variants").load(url + ' '+ '.product-variants')
			$("#quantity").load(url + ' '+ '.product-quantity-input')
		$("#cartBtn").load(url + ' '+ '.sqs-add-to-cart-button')
 // if (status == "error") {
 //    var msg = "Sorry but there was an error: ";
 //    alert(msg + xhr.status + " " + xhr.statusText);
 // }
// });

});
